{
  "name": "todo-list-app",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start-backend": "cd backend && npm start", // Start the backend
    "start-frontend": "cd frontend && npm start", // Start the frontend
    "start": "npm run start-backend & npm run start-frontend", // Start both backend and frontend together
    "install-backend": "cd backend && npm install", // Install backend dependencies
    "install-frontend": "cd frontend && npm install", // Install frontend dependencies
    "install": "npm run install-backend && npm run install-frontend" // Install all dependencies for both
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}

